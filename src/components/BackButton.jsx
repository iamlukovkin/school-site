import React from "react";

export default class BackButton extends React.Component {
    render() {
        return (
            <a href={"/"}>
                <button>Назад</button>
            </a>
        )
    }

}